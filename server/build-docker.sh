#!/bin/bash
# tag=$( git describe --abbrev=0 )

# echo $DOCKER_PASSWORD | docker login --username bingneef --password-stdin
# docker build -t bingneef/docker-snowflake:$tag .
# docker push bingneef/docker-snowflake:$tag